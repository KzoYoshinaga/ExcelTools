Private Function test()

    Sheets("pile").Cells.Clear
    Sheets("zip").Cells.Clear
    Sheets("test").Cells.Clear

    Dim t As New table

    Call t. _
          import(Sheets("A")). _
          skipPile(t.import(Sheets("B")), 4). _
          export(Sheets("pile").Range("A1")). _
          terminate

    Call t. _
          import(Sheets("A")). _
          skipZip(t.import(Sheets("B")), 2). _
          export(Sheets("zip").Range("A1")). _
          terminate


End Function
